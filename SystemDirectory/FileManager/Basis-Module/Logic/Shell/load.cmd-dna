#!/bin/bash
# load.core-bash-dna
# Lädt Datei aus dem Internet via wget

DNA_EXEC() {
    if [ -z "$1" ]; then
        echo "🧬 DNA load: Bitte eine URL angeben."
        echo "Nutzung: dna load https://example.com/datei.zip"
    else
        echo "🧬 DNA load: Lade $1 ..."
        wget "$1"
    fi
}
