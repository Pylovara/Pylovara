# off.core-bash-dna
# Entfernt +x von Datei(en)

DNA_EXEC() {
    for file in "$@"; do
        if [[ -f "$file" ]]; then
            echo "🧬 DNA off: chmod -x '$file'"
            chmod -x "$file"
        else
            echo "❌ Datei '$file' nicht gefunden."
        fi
    done
}
