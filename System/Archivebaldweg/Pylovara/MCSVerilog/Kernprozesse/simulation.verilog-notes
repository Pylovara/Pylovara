##Vorgehensweise + ergebnise + änderungen + analysen etc ....

Mon 27.oktober
TimeFehler : "Zustandsmaschine optimiert, Timing synchronisiert".

Analyse nach dem ändern :

#5 (nach send):
data_out (%): bx bis #60, dann b10101010 = 0xAA, Verzögerung.
valid (!): 0 bis #50, dann 1 bei #60, Verzögerung.

#25 (nach copy):
data_out (%): bx bis #70, dann b11001100 = 0xCC, Verzögerung.
valid (!): 0 bis #70, dann 1, Verzögerung.

#45 (nach to):
data_out (%): bx bis #60, dann b11011101 = 0xDD, Verzögerung.
valid (!): 0 bis #60, dann 1, Verzögerung.

Beobachtung: Die Zustandsmaschine verarbeitet jetzt korrekt, aber es gibt eine Verzögerung von etwa 10–20 Zeiteinheiten, weil die Ausgabe erst nach einem weiteren Taktzyklus gesetzt wird.
Die Verzögerung von einem Taktzyklus passt zum synchronen Design und könnte später mit einem ⊕sync-timer optimiert werden, um parallele Einspeisungen zu unterstützen.


->Ergänze simulation.verilog-notes mit "Simulation synchron, Verzögerung akzeptiert, echo geplant"

Änderung in mcs_protein.v: Eine Bedingung wurde für echo hinzugefügt.
cd /Pylovara/MCSVerilog/Proteine
nano mcs_protein.v der always block wurde aktualisiert

konkretter ausschnitt : if (cmd_reg == 8'b00000001) begin // echo

-> Ergänze simulation.verilog-notes mit "Echo validiert, Vorbereitung auf send mit Dateipfaden".
initial block :

        // In mcs_sim_tb.v, nach #20 wobei doppelt #5 zur info :
        #25 cmd = 8'b00000010; src_addr = 32'h00000000; dst_addr = 32'h00000010; data_in = 8'hAA; executable = 1'b1;

Verilog-Implementierung
Erweiterung von mcs_protein.v, um den ⊕ Sync-timer als Zustand hinzuzufügen im always-Block
-> ergänze : bit größe auf 2:0 gesetzt 5 ausführungen, synctimer :
-> Simulation: Mit der korrigierten Datei sollte der Sync-Timer bei #25 (cmd = 8'b00000101, data_in = 8'h19) korrekt data_out = 0x19, src_addr_out = 0x00000001, dst_addr_out = 0x00000002 ausgeben.
Ergänze simulation.verilog-notes mit:

"Simulation synchron, Verzögerung akzeptiert, echo geplant"
"Echo validiert, Vorbereitung auf send mit Dateipfaden"
"STATE_SYNC mit 3 Bits implementiert, Sync-Timer-Test erfolgreich validiert bei #90 mit data_out = 0x19, src_addr_out = 0x00000001, dst_addr_out = 0x00000002."


die Parallelität mit STATE_SYNC wurde erfolgreich eingeführt – das ist ein großer Schritt

vermerk optionale gedanken :
Optimierung: Die Verzögerung könnte mit einem ⊕sync-timer (z. B. ein Zähler in STATE_SYNC) reduziert werden.

#########################################################################
