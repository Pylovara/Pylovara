Was „/“ wirklich ist

/ = Root eines Namensraums

Minimal bedeutet:

ein VFS-Root-Pointer

keine Pfade

keine Strings

keine „Verzeichnisse“ im UX-Sinn

Intern z. B.:

struct node {
    enum { FILE, DIR } type;
    struct node* parent;
    struct node* children[];
};


/ ist einfach:

root_node


Nicht mehr. Nicht weniger.
