¢!
# 1. Prüfen ob Prozess existiert
»['pgrep sshd']«
  # Wir speichern den Output (die PID) in Register 1
  # Box leer lassen = nimm letzten Shell-Output!
  -= »[(1)¨¨]«

# 2. Sentiator-Entscheidung
# ¶¶ triggert, wenn Register 1 leer ist (Prozess läuft NICHT)
¶¶ »[¨(1)¨]«
    »['echo "WARNUNG: sshd nicht gefunden. Starte neu..."']«
    »['systemctl restart sshd']«
    –·: ############################### 100% »['echo "Restart-Impuls gesendet."']«

# ¶ triggert, wenn Register 1 Daten enthält (Prozess LÄUFT)
¶ »[¨(1)¨]«
    –·: ############################### 100%
   »['echo "System stabil. sshd läuft auf PID: ¨(1)¨"']«
!¢
