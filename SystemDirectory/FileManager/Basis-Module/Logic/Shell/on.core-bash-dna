# on.core-bash-dna
# Aktiviert chmod +x für Datei(en)

DNA_EXEC() {
    for file in "$@"; do
        if [[ -f "$file" ]]; then
            echo "🧬 DNA on: chmod +x '$file'"
            chmod +x "$file"
        else
            echo "❌ Datei '$file' nicht gefunden."
        fi
    done
}
