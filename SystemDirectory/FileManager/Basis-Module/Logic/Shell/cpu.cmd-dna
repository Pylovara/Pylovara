#!/bin/bash
# cpu.cmd-dna
# Zeigt CPU-Informationen (via lscpu)

DNA_EXEC() {
    if ! command -v lscpu >/dev/null 2>&1; then
        echo "❌ Fehler: 'lscpu' ist nicht installiert" >&2
        return 1
    fi
    echo "🧬 DNA cpu: Zeige CPU-Infos (lscpu)"
    lscpu | less
}
