¢!
# Datei : test_semnatic_dispatcher.mcs

»['echo'|¨1¨|(99)¨$echo OK¨]«   # Feed 99 = "OK"
 ° [(99)¨¨]   # liest Feed 99 → nicht leer → MCS_TRUE → ° negiert → MCS_FALSE
  ¶ »['echo'|'Feed 99 war leer']«
!¢

um es auch eindeutig mit operatoren zu handlen wäre es sinn voll ,
wenn ein operator ein protein anhang logik folgen soll auch immer am anfang eines protein packets [ die regeln anzuheften ...

  damit das auch ein schaltkreis ergibt das selbe gilt dann auch für wahrheiten

  nur direkt unter dem anfang eines protein packets , gehört etwas zu dem protein header , damit das dann auch eine schalt logik hat


